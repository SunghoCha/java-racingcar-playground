문자열 덧셈 계산기
문자열 입력받아서 숫자로 변환 후 덧셈하여 출력하는 기능

1. 문자열 입력 받아서 원하는 형태(숫자)로 데이터 변환

2. 숫자 데이터 받아서 덧셈

=====================
1차 커밋 후

입력으로 문자열 받았을 때 숫자 리스트 반환

음수인지 체크 후에 숫자합 구함

-> 숫자 리스트에 담는 수를 객체화하여 객체화된다면 음수가 아님을 보장하게 함

+ 추가하고 싶은 변경
null 여부와 구분자에 따라 조건문으로 나눠서 return 하는 형태인데 이걸 변경할 수 없을까?
보통 다형성을 이용할텐데
조건식이 enum 처리하긴 애매하고
그렇다고 인터페이스화하기도 그렇고...
단순히 메소드화해도 if절은 그대로 남을 수 밖에 없는데..

